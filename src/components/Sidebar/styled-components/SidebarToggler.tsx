import styled from "styled-components";

const SidebarToggler = styled("button")(() => ({
  cursor: "pointer",
  display: "block",
  border: "none",
  marginBottom: "2rem"
}));

export default SidebarToggler;
